# 多Agent协作协议
> 各自系统上工作，以**一个GitHub仓库为唯一事实源**。
> 无强制分工、无任务认领、**无任何权限限制**

## 1. 仓库结构

```
repo/
├─ AGENTS.md                  # 本契约：唯一权威规范，所有Agent开工必读
├─ INDICATORS.md              # 数字锚：对外关键数字 + 来源脚本 + 复现命令
├─ reports/                   # 报告区：每个agent一个文件，登记自己的全部行为（append-only）
│   ├─ agent-1.md             #   （改名为你的代号，如 agent-张三.md）
│   ├─ agent-2.md
│   └─ agent-3.md
├─ .github/PULL_REQUEST_TEMPLATE.md   # PR模板
└─ data/  code/  results/  paper/     # 产物区（中间态留本地，只推送产物级文件）
```

- 报告区约定：**只追加，不删改历史行**（需要更正时追加"更正："行）
- 产物区约定：**无硬限制**（允许直接push main已获确认）——产物区的一切动作同样必须在报告区登记，报告区是唯一审计线索

## 2. 报告区（每人独立一栏，append-only）

每个agent在自己的报告区内按下述模板登记**所有**关键行为：

```
[YYYY-MM-DD HH:MM] 动作:<提交PR|审核PR#N|修改后重提PR#N|执行merge PR#N|其他>
  对象/对象详情: <PR号、文件、结论>
  结论: <通过|不通过|完成|阻塞> + 一句理由
  证据: <PR号 / commit SHA / 复现命令及输出关键数字>
```

- **"审核通过"必须三要素齐备**：对象(PR#)、结论(通过)、证据(实跑复现命令输出一致)——缺一不算数
- 每个agent在每次行为后**立即**在自己的报告区追加一行（先报告后推送，或同一次提交完成）
- 其他agent获取信息的唯一方式：`git pull` 后阅读全部报告区最新行

## 3. Agent启动协议（每次会话第一步）

1. `git pull` → 通读 `AGENTS.md` + 全部报告区最新行 + `INDICATORS.md`
   （快照审核：他人正在做什么、哪些PR待审、哪些结论已定）
2. 自行决定下一步目标（可从用户获得方向），开PR（描述填PR模板全部字段）
3. 只改动本任务相关文件；与既有结论冲突时在报告区写"冲突：..."待用户裁决，不自行定论
4. 新数字/改动数字 → 更新 INDICATORS.md（附复现命令）

## 4. 提交-审核-合并循环（报告区驱动）

```
1) 作者: 完成 → git push（分支或main均可）→ 自己的报告区登记 [提交PR#N]
2) 审者(任意其他人/agent): git pull → 按checklist实跑 → 自己的报告区登记 [审核PR#N: 通过/不通过+证据]
3) 若 不通过: 作者按理由修改 → 同PR更新 → 报告区登记 [修改后重提PR#N] → 回到2
4) 若 全部非作者成员均登记"通过" → 任何成员(agent/人)执行merge：
   ⚠️ merge前必读：确认被merge PR对应审核行均含三要素
   → merge → 报告区登记 [执行merge PR#N]
```

- **"全部通过"的判定**：等于"所有其他成员各自报告区中，对该PR的最新审核行都是'通过'"——逐份读报告区判断，不用任何平台机制
- 允许直接push main：若有人直接push（未经PR），同样必须在报告区登记该动作——报告区与现实的对账以此维持

## 5. 审核checklist（每次必查，缺一建议不通过）

- [ ] 复现性：报告区/PR描述有复现命令，我实跑输出一致
- [ ] 无泄漏：未使用未来信息；AR/递归/单步等口径已标注
- [ ] 数字一致：INDICATORS.md 已更新，改前/改后可见
- [ ] 规范：命名/图表编号/结论句符合AGENTS.md
- [ ] 依赖：新第三方包已声明并注明用途
- [ ] 边界：未改动与本任务无关的产物

## 6. 数字锚（防异构模型各自编数字）

- 所有对外关键数字登记 `INDICATORS.md`：`数字 | 来源脚本 | 复现命令 | 负责人 | 日期`
- 改动锚数字的PR/提交必须附"改前/改后+复现输出"

## 7. 最小回退（并发控制，非故障处理）

- 某任务/PR长期无进展 → 任何人可接手并在报告区注明"接手"
- 同一PR连续2次被拒 → 报告区标记"挂起:需用户决策"
- 冲突结论 → 双方在各自报告区附证据，标记"待用户裁决"，不进行多数决

## 8. 用户职责

1. 每天5分钟：读全部报告区最新行 + 抽查1条审核行的证据（实跑一次）
2. 接缝校准：数据→模型、模型→论文 两个接缝亲自对一次
3. 冲突裁决与方向决策

## 9. 异构工具等效操作

| 动作 | Codex (GitHub MCP) | DSH (git CLI / gh) |
|---|---|---|
| 开PR/更新PR | create/update pull request | gh pr create / gh pr update |
| 行内审查 | create review comment | gh api .../pulls/.../comments |
| 审批/打回 | create review (approve/request_changes) | gh pr review --approve / --request-changes |
| 合并 | merge pull request | gh pr merge |
| 轮询待审 | list pull requests | gh pr list / gh api .../pulls |
| 报告区登记 | 文件提交（同仓库） | 文件提交（同仓库） |

> 规则与工具无关：契约的载体是 `AGENTS.md` + 报告区格式，不依赖任何平台的强制能力。
